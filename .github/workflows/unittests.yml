name: C/C++ CI
on:
    push:
        branches: [ main ]
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - name: make and run unittests
          run: |
            make unittests
            ./unittests.exe
